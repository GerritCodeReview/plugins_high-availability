[access]
	inheritFrom = Public-Plugins
[access "refs/*"]
	owner = group plugins-high-availability
	push = +force group google/gerritcodereview-maintainers@googlegroups.com
[project]
	description = Synchronize eviction of caches, secondary indexes and stream events between two Gerrit instances sharing the same git repositories and database.
